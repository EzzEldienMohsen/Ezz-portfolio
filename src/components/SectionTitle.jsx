/* eslint-disable react/prop-types */
const SectionTitle = ({ text }) => {
  return (
    <div className="border-b border-gray-500 pb-5">
      <h2 className="capitalize font-medium tracking-wide text-3xl">{text}</h2>
    </div>
  )
}

export default SectionTitle
